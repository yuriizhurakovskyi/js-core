hideItemButton.onclick = function() {
	if (hideItemButton.value == 'Hide the button')
		hideItemButton.style.visibility = "hidden";
	text.style.visibility = "hidden";
	hideItemButton.value = 'Hide the button';
}